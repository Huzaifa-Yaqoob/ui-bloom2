import { PropsType } from '@/components/common/PropsTable';

export const propsData: PropsType[] = [
  {
    propName: 'data',
    propType: 'PropsType[]',
    defaultValue: '[]',
    description: 'Array of props data for the table',
  },
];
